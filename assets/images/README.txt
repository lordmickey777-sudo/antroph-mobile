Place app images here:

- app_logo.png  -> Used on Splash screen (centered)
- eye_logo.png  -> Used on Onboarding screens (large, top-left)

Recommended sizes:
- app_logo.png: 512x512 or similar, transparent background
- eye_logo.png: at least 1024x1024 to allow cropping/overflow

After adding files, run:
  fvm flutter pub get

